#!/usr/bin/env node
var debug = require('debug')('SUPN');
var server = require('../server');

server.set('port', process.env.PORT || 3000);

var listener = server.listen(server.get('port'), function () {
    debug('Server listening on port ' + listener.address().port);
});
